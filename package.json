{
  "name": "block-chain-wallet-explorer",
  "version": "1.0.0",
  "npmClient": "yarn",
  "private": true,
  "workspaces": [
     "packages/*"
  ],
  "scripts": {    
     "start:client": "yarn workspace client start",    
     "start:api": "yarn workspace api start:dev",    
     "build:client": "yarn workspace client build"  
  }
}
